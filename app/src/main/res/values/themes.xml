<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Handrail" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/handrail_bg</item>
    </style>

    <style name="Theme.Handrail.Translucent" parent="Theme.Handrail">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!--
      HandrailInteractionSession's window (a Dialog, per VoiceInteractionSession's
      own internals). Floating + no dim + transparent background is what keeps
      the host app visible and un-dimmed underneath the invoke sheet — see the
      session's own doc.
    -->
    <style name="Theme.Handrail.InvokeSession" parent="android:Theme.DeviceDefault.Light.Dialog.NoActionBar">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowFullscreen">false</item>
    </style>
</resources>
