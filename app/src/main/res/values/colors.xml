<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="ic_launcher_background">#101418</color>

    <!-- Classical design system ground — see ui/theme/HandrailColors.kt. Kept
         in sync manually; used only where a resource-level color is required
         (the window background, so there's no white flash before Compose
         draws) rather than the Kotlin token, which Compose code should use. -->
    <color name="handrail_bg">#F3F2F2</color>
</resources>
